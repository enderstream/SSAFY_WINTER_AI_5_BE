# Core Python Libraries
attrs==24.3.0
decorator==5.1.1
typing_extensions==4.12.2
platformdirs==4.3.6

# Web Frameworks
Django==4.2.8  # Django 웹 프레임워크
djangorestframework==3.14.0  # Django용 REST API 프레임워크
fastapi==0.115.6  # FastAPI 웹 프레임워크
starlette==0.41.3  # FastAPI의 핵심 ASGI 라이브러리

# HTTP and Networking
aiohttp==3.11.11  # 비동기 HTTP 클라이언트
httpx==0.28.1  # HTTP 클라이언트 (Requests 대안)
requests==2.32.3  # HTTP 요청 라이브러리
urllib3==2.2.3  # HTTP 클라이언트의 핵심 라이브러리

# AI and Language Models
openai==1.58.1  # OpenAI API 클라이언트
langchain==0.3.13  # LLM 애플리케이션 개발 도구
langchain-core==0.3.28
langchain-openai==0.2.14
langchain-text-splitters==0.3.4
langchain-upstage==0.4.0
langsmith==0.2.7

# Pinecone (벡터 데이터베이스)
pinecone==5.4.2  # Pinecone 클라이언트 라이브러리
pinecone-plugin-inference==2.0.0

# Data Science and Machine Learning
numpy==2.1.2  # 수치 계산 라이브러리
pandas==2.2.2  # 데이터 분석 라이브러리
scipy>=1.10  # 과학 계산 라이브러리 (필요 시 추가)
matplotlib==3.9.1  # 데이터 시각화 라이브러리

# PDF and File Handling
pypdf==4.3.1  # PDF 처리 라이브러리

# Utilities and Tooling
python-dotenv==1.0.1  # 환경 변수 로드 도구
tenacity==9.0.0  # 재시도 로직 구현 도구
tqdm==4.67.0  # 진행률 표시줄

# ASGI Server for FastAPI/Django
uvicorn==0.34.0  # ASGI 서버 (FastAPI/Django 사용)

# Development Tools and Debugging
debugpy==1.8.6  # 디버깅 도구 (VS Code 호환)
ipython==8.18.1  # 대화형 Python 셸

# Miscellaneous Libraries
PyYAML==6.0.2  # YAML 파일 처리 라이브러리
regex==2024.11.6  # 정규 표현식 처리 라이브러리

